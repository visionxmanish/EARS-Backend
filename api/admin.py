# Admin registrations are handled by the existing project's apps
# (authentication/admin.py and reports/admin.py)
# This file is kept empty to avoid duplicate registrations

# If you need to customize admin for the API project, you can:
# 1. Unregister models first: admin.site.unregister(Model)
# 2. Then register with your custom admin class
